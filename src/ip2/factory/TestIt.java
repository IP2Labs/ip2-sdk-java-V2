package ip2.factory;


import ip2.utils.IP2Constants;

import java.util.HashMap;

import org.junit.Test;

public class TestIt {

	@Test
	public void something()
	{
		/*AbstractFactory abstractFactory = FactoryProducer.getFactory();
        //PaymentReference paymentReference = (PaymentReference)abstractFactory.newInstance(Reference.PAYMENT_REFERENCE.ordinal());
        //ChannelReference cf = (ChannelReference)abstractFactory.newInstance("ChannelReference");
        
        ChannelReference r = new ChannelRef();
        
        HashMap<String, String> map = new HashMap<String, String>();
        map.put("PhoneNumber", "0784703425");
        
        paymentReference.setPaymentReference(map);
        ///cf.setChannelReference(map);
        
        HashMap<String, String> newMap = paymentReference.getPaymentReference();
        
        //System.out.println(cf.getChannelRefence().get("PhoneNumber"));
*/	     
	}
}
